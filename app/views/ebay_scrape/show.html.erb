<% if @results.any? %>
	<div class="ebay-scrape-info-container">
		<%= render partial: 'ebay_scrape_info', locals: { ebay_scrape: @ebay_scrape } %>
	</div>

	<h1>Results</h1>
	<div class="results-table-container">
		<%= render partial: 'results_table', locals: { results: @results } %>
	</div>

	<% if !@ebay_scrape.finished_updating? %>
		<script>
			$(document).ready(function() {
				function updateEbayScrape() {
					$('body').load('/ebay_scrape/' + $('.ebay-scrape-info').data('scrape-id') )
				}
				setTimeout(updateEbayScrape, 3000)
			});
		</script>
	<% end %>
<% else %>
	<p class="text-center">No results for this search. Please <%= link_to "go back", '/' %> and refine your search</p>
<% end %>